# Problem Title — Solution

## Intuition
[The key insight — why this approach fits the problem.]

## Algorithm Explanation
[Explain the approach and the steps — the logic that makes the algorithm work.]

## Dry Run
[Trace the algorithm step by step on a concrete example input, using the representation that best
fits the data structure (don't default to a table) — see "Dry-run visualizations" in AGENTS.md.
Required: the visual is what makes it stick.]

## Complexity
- Time: O(...)
- Space: O(...)

## Pitfalls & Edge Cases
[Tricky inputs, boundaries, and common mistakes to watch for.]
